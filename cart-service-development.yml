server:
 port: 8100
#  port: ${random.int(8100,8199)}

user:
  rolesss: dev
  
management:
  endpoints:
    web:
      exposure:
        include: "*"

logging:
  level:
    org:
      springframework:
        security: DEBUG 

