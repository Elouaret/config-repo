server:
 port: 8100
#  port: ${random.int(8100,8199)}

user:
  role: dev-role-16
  
management:
  endpoints:
    web:
      exposure:
        include: "*"

logging:
  level:
    org:
      springframework:
        security: DEBUG 

