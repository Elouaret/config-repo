user:
  role: default-role-4

management:
  endpoints:
    web:
      exposure:
        include: "*"
    sensitive: true
    actuator:
        enabled: true
  endpoint:      
    env:
      post:
        enable: true
    restart:
      enabled: true  
    refresh:
      enabled: true  
      
logging:
  level:
    org:
      springframework:
        security: DEBUG

